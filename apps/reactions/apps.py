"""Reactions app configuration."""

from django.apps import AppConfig


class ReactionsConfig(AppConfig):
    """Configuration for the Reactions app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.reactions'
